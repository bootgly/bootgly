<?php

namespace projects\Bootgly\WPI\examples\app;

# $this->Template = new Template($this->Web);

$Router->boot(__DIR__, [
   'routes',
   #'routes.requesting',
   #'routes.responsing',
   #'routes.templating'
]);
